# Git aliases
alias gs='git status'
alias gc='git add . && git commit -m'
alias gps='git push'
alias gpl='git pull'
alias cdsp='claude --dangerously-skip-permissions'

# Load .env if present
set -a && source /workspaces/olmo/.env 2>/dev/null && set +a
